This folder is used for anything data related, file type does not matter, but I better not see a .py in here.

It should contain:

BreedHerdYear
FunctionalConsequences
Phenotypes
RawData
TopSNPs
output_hd_exclude_binary_herd.txt

I do not know what this file is:

output_hd_exclude_binary_herd.txt